{
	"name": "creating-a-database",
	"version": "1.0.0",
	"description": "",
	"main": "app.js",
	"type": "module",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon -r dotenv/config app.js",
		"start": "node -r dotenv/config app.js",
		"createTable": "node -r dotenv/config ./db/scripts/createTable",
		"populateTable": "node -r dotenv/config ./db/scripts/populateTable"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"nodemon": "^2.0.15"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^16.0.0",
		"express": "^4.17.1",
		"firebase-admin": "^10.0.2",
		"pg": "^8.7.1"
	}
}
